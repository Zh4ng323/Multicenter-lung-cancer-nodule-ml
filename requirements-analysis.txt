# Analysis-side dependencies (subset of environment.yml).
# The web app has its own, fully pinned requirements.txt at web_app/requirements.txt.
# Pinned to the versions the locked SVM was trained/saved under — do not bump
# scikit-learn / pandas without re-validating (see docs/REPRODUCIBILITY.md).
numpy==1.24.4
pandas==1.4.4
scikit-learn==1.3.2
scipy==1.10.1
statsmodels==0.14.1
matplotlib==3.6.3
seaborn==0.12.2
xgboost==2.1.2
lightgbm==4.5.0
shap==0.44.1
joblib==1.4.2
